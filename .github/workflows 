verfy.yml
name: ProofGate Verification

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
-name: ProofGate Verification

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:   # manual trigger option

permissions:
  contents: read       # allow reading repo files
  checks: write        # allow posting status checks
  actions: none        # prevent modifying other workflows
  packages: read       # safe default, no publishing

jobs:
  verify-digests:
    runs-on: ubuntu-22.04   # pinned runner for stability
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
-jobs:
  zero-trust-verifier:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run ZeroTrust verifier
        run: |
          python3 scripts/zero_trust_verify.py

      - name: Enforce QUITNLOCK protocol
        run: |
          python3 scripts/quitnlock_enforce.py
      - name: Verify SHA digests with Python
        run: python3 scripts/verify_capsule.py
      - name: Verify SHA digests
-name: ZeroTrust + QUITNLOCK Enforcement

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:   # manual trigger option

permissions:
  contents: read       # allow reading repo files
  checks: write        # allow posting status checks
  actions: none        # prevent modifying other workflows
  packages: read       # safe default

jobs:
  verify-digests:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Verify SHA digests with Python
        run: python3 scripts/verify_capsule.py

  zero-trust-verifier:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run ZeroTrust verifier
        run: python3 scripts/zero_trust_verify.py

  quitnlock-enforce:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Enforce QUITNLOCK protocol
        run: python3 scripts/quitnlock_enforce.py
        run: |
          echo "🔒 Checking SHA-256 and SHA-512 digests..."
          sha256sum capsule_manifest.json > digest.sha256
          sha512sum capsule_manifest.json > digest.sha512
          diff digest.sha256 expected.sha256
          diff digest.sha512 expected.sha512

      - name: Verify Canonical Signing Wallet
        run: |
          echo "🔒 Checking canonical signing wallet..."
          curl -s "https://api.etherscan.io/api?module=account&action=txlist&address=0x9e223F20022580739060d5511445C727f51Ee81e" \
  -name: ProofGate Verification   # or Zeppelin Inspection

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:   # optional manual trigger

permissions:
  contents: read       # allow reading repo files
  checks: write        # allow posting status checks
        | grep -q "status" && echo "✅ Canonical wallet verified"

      - name: Verify On-Chain Proof Wallet
        run: |
          echo "🔒 Checking on-chain proof wallet..."
          curl -s "https://api.etherscan.io/api?module=account&action=txlist&address=0x65f01815D18787e975D3439f480d49e9cb8E98F2" \
          | grep "320378" || (echo "❌ Signature #320378 not found" && exit 1)
- name: DAO Seal Verification
        run: |
          echo "🛡 MintProofDAO Seal: 436351437"
          echo "🛡 LedgerProofDAO Seal: 430"
      - name: ProofGate Enforcement
-GITHUB_TOKEN:permissions:
  contents: read
  checks: write 
        run: |
   - name: Use secret
  run: echo "${{ secrets.MY_SECRET }}"       echo "✅ ProofGate enforcement active: Zero-Trust Verifier + QUITNLOCK deterministic locks"